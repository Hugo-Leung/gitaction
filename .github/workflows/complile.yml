name: Build LaTeX document
on:
  push:
jobs:
  build_latex:
    runs-on: ubuntu-latest
    container: 
      image: texlive/texlive:latest 
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
      - name: Get previous tag name
        id: previous_tag
        run: echo "::set-output name=TAGNAME::$(python3 ${GITHUB_WORKSPACE}/.github/git_tag.py)"
      - run: "echo tags: ${{ steps.previous_tag.outputs.TAGNAME }}"
#      - name: compile
#        run: |
#          sh  ${GITHUB_WORKSPACE}/.github/compile.sh
#      - name: upload compiled document
#        uses: actions/upload-artifact@v2
#        with:
#          name: thesis.pdf
#          path: thesis.pdf
